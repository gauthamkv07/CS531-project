##################################################
# Make File for run Linked List Program
##################################################

OBJ = main.o

all: $(OBJ)
	gcc -o all $(OBJ)
main.o: main.c structures.h
	gcc -c main.c
clean:
	rm -rf a.out
	rm -rf all
	rm -rf $(OBJ)
